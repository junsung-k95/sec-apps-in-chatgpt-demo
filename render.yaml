services:
  - type: web
    name: samsung-galaxy-mcp
    runtime: node
    rootDir: samsung-galaxy-mcp/server
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: "8787"
      - key: WIDGET_DOMAIN
        sync: false
